@extends('adminlte::page')

@section('content_header')
    <h1>Visual</h1>
@stop

@section('template_title')
    Visual
@endsection

@section('content')
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="card">
                    <div class="card-header">
                        <div style="display: flex; justify-content: space-between; align-items: center;">

                            <span id="card_title">
                                {{ __('Visual Document') }}
                            </span>

                        </div>
                    </div>
                    @if ($message = Session::get('success'))
                        <div class="alert alert-success">
                            <p>{{ $message }}</p>
                        </div>
                    @endif

                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead class="thead">
                                    <tr>
                                        <th>No</th>
                                        
										<th>Name</th>
										<th>Organization</th>

                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach ($roles as $role)
                                        <tr>
                                            <td>{{ ++$i }}</td>
                                            
											<td>{{ $role->name }}</td>
											<td>{{ $role->organization_id }}</td>

                                            <td>
                                                    <a class="btn btn-sm btn-primary " href="{{ route('visual.show',$role->id) }}"><i class="fa fa-fw fa-eye"></i> Show Visual</a>
                                                    <a class="btn btn-sm btn-success" href="{{ route('visual.edit',$role->id) }}"><i class="fa fa-fw fa-edit"></i> Edit Visual</a>
                                            </td>
                                        </tr>
                                    @endforeach
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
